# Public Participation and Deliberation Plans

Store one dated JSON plan for each major proposal, using `templates/public-deliberation-plan.example.json`. A plan must make public participation accessible, informed, safe, and resistant to capture. It must also show how decision-makers responded to public input.

Run `python3 scripts/validate_public_deliberation.py <plan.json>` before publishing a plan.
